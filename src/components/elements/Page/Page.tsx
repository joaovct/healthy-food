import styled from 'styled-components'

const Page = styled.section`
    flex: 1;
    width: 100%;
    display: flex;
    flex-flow: column nowrap;
    align-items: center;
    padding: var(--PageVerticalSpacing) var(--PageHorizontalSpacing);
`

export default Page